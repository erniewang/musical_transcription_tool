pitchlab will be a adaptive transcription model package that features 
mutiple models. such as pyin, librosa-pyin, yourmpt, yaapt, pyin, torchcrepe, essential melodica

the folder structure will be as the following

pitch_lab_ernie -> main.py , model_folders


the main file is as follows: 
import every single run model function from the individual folders

class pitch_lab:
    models = "all the models 

    #core functions 
    def __init__:
        instantiate the class and the properties. verify all models are loaded in.

    def run_model(
        model_name : str, 
        input_audio : (not a csv or a path, the actuall audio) , 
        parameters : {}
        and model specific parameters are at the disction of the model 
        desired_output: can be empty for default, csv, pandas dataframe, midi, sonified audio
        the output will be whatever the model prints out. it will use (convert_data) to find the desired output

    def run_all_models(audio_file, parameters: {} ,except_models: a list of models that you do not want to be run):
        runs all models not in except_models in succession

    #utility functions
    def print_all_model_specifications():
        lists every model and specific parameters of. but it should be in this recommended order. 

    def print_specifiations(model:str): prints the specifications of a single model

    def _convert_data(data, desired_form): converts it into csv, pandas_df, mini, sonfidied sine wave, etc

    
for every run_(model_name).py file the following function will be 

import whatever that model needs for it to work
def run_(model_name), input_audio (unchanged just drilled down a layer), parameters:
    run the algorithm with that model and with the specificed parameters. 
    if the model does not accept those exact parameters, then give a warning that the model does not accept it but continues to run it 
        without the parameter in question. 
