#loading in a listening to original audio block
import pitchlab, and everything else

load in the audio. 

display audio to play unaltered. with the display preferabbly using just seconds like 69 seconds instead of 1:09.

#decide which sections of the audio are to get sliced. 

for example. [0, 27, 43, 56, 78, 89, -1] it will get sliced like (0,27) (27,43)... and (89 seconds to the end)
so there will be 6 distinict sections in this example. and there will be corrospoinding information for each section

#preprocess
HIGH_PASS = [(135),(200)..etc

here pre-processing filters will be applied to each section of the video, such as lowpass or highpass. and . the format is similar with 
for section in sections:
    #run preprocessing over here


#algorithm filters. 

SECTION_FILTER_RANGE_HZ = [(135,269), (215,300), (135,269), (208,415), (215,300), (135,269)] is one example, there could be other filters that will also be in this format
display audio of each section

there may be more filers here that will of a array of size 6 with each instance a filter for that section

#run every section through every odel 
MODEL_LIST = ["pyin","essentia","torchcrepe"....]

for section in sections:
    for model in MODEL_LIST:
        #runs the model
        save(path / model / (original_name)_(section_number)_(model_used).csv 

